<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title Add Post
    body
        h2 Add Post
        br
        form(action="http://localhost:8000/post" method="POST" enctype="multipart/form-data")
            label(for="text") Post Text
            input(type="text" name="text" max="200")
            br
            label(for="likes") Likes
            input(type="number" name="likes")
            br
            label(for="likes") User ID
            input(type="number" name="username")
            br
            button(type="submit") Submit